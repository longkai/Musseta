<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="yuejia.liu.musseta">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

  <application
      android:name=".Musseta"
      android:allowBackup="true"
      android:label="@string/app_name"
      android:theme="@style/Musseta">

    <activity android:name=".ui.BootstrapActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <meta-data android:name="com.crashlytics.ApiKey" android:value="ba2cdccffa22a0324bebda13dde369e2c61847af"/>
  </application>
</manifest>
